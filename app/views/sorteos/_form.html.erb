<%= form_for(@sorteo) do |f| %>
  <% if @sorteo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sorteo.errors.count, "error") %> prohibited this sorteo from being saved:</h2>

      <ul>
      <% @sorteo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<p><%= f.label :titulo %>
	<%= f.text_field :titulo %></p>
<p><%= f.label :descripcion %>
	<%= f.text_field :descripcion %></p>
<p><%= f.label :numero_de_boletos %>
	<%= f.text_field :num_boletos %></p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
